# Windows 防沉迷管理软件实施计划

## 一、项目背景

计划开发一款运行于 **Windows 平台** 的本地防沉迷管理软件，用于限制电脑整体使用时间，或针对指定应用、游戏配置每日可用时长。

项目的首要使用场景是控制游戏类程序的使用时间，例如 **QQ 游戏大厅、野狐围棋** 等。软件需要能够在后台持续运行，对目标程序的启动和运行状态进行监控，并根据管理员配置的时间额度进行统计、提醒和限制。

本项目仅考虑 **Windows 10 / Windows 11**，不考虑 Linux、macOS、Android、iOS 等其他平台。

---

## 二、项目目标

项目目标是开发一个简单、可靠、低资源占用的 Windows 防沉迷管理工具，具备两种核心限制模式：

### 1. 整机使用时长限制

可以配置当前 Windows 用户每天允许使用电脑的总时长，例如：

- 工作日每天允许使用 2 小时
- 周末每天允许使用 4 小时
- 不同星期可以设置不同额度
- 达到额度后自动锁定或限制继续使用电脑

适用于希望整体控制电脑使用时间的场景。

### 2. 指定应用使用时长限制

管理员可以配置一个或多个需要限制的应用，例如：

- QQ 游戏大厅
- 野狐围棋
- Steam
- 某个具体游戏
- 浏览器
- 视频软件

软件根据目标程序实际运行时间累计使用额度。

例如：

```text
野狐围棋

每日额度：2小时

匹配进程：
foxwq.exe
foxwqclient.exe
```

达到每天 2 小时后：

```text
关闭当前游戏
+
当天禁止再次运行
```

第二天自动恢复新的时间额度。

---

## 三、核心设计原则

### 1. 管理界面与限制服务分离

整个软件分为两个主要部分：

```text
┌──────────────────────┐
│   防沉迷管理客户端     │
│                      │
│ 设置规则             │
│ 查看剩余时间         │
│ 查看使用记录         │
│ 修改额度             │
└──────────┬───────────┘
           │
           │ IPC
           ▼
┌──────────────────────┐
│ Windows 后台服务      │
│                      │
│ 进程监控             │
│ 时间统计             │
│ 规则判断             │
│ 应用终止             │
│ 整机锁定             │
└──────────────────────┘
```

Windows Service 是真正负责执行限制的核心。

GUI 仅负责配置和展示。

这样即使关闭管理界面，限制功能仍然继续工作。

---

## 四、主要功能范围

### 1. 防沉迷规则管理

支持创建多条限制规则。

规则类型包括：

```text
整机限制
指定应用限制
```

每条规则可以配置：

- 规则名称
- 是否启用
- 规则类型
- 每日时间额度
- 生效星期
- 是否允许管理员临时增加时间
- 时间耗尽后的处理方式

例如：

```text
规则名称：游戏限制

类型：应用限制

周一至周五：
2小时

周六至周日：
4小时
```

---

## 五、整机限制功能

整机限制主要控制当前 Windows 用户每天使用电脑的总时间。

需要实现：

### 使用时间统计

用户登录后开始计时。

可以根据实际方案决定：

- Windows 登录即开始计算
- 用户有操作时才计算
- 锁屏状态暂停计算
- 睡眠状态暂停计算

建议默认采用：

```text
用户登录
+
电脑未锁屏
+
电脑处于活动状态
```

才计入有效使用时间。

### 时间耗尽处理

可以提供以下策略：

```text
仅提醒
锁定 Windows
注销当前用户
```

第一版建议主要支持：

**锁定 Windows。**

相比关机或强制注销，对用户正在进行的工作影响更小。

---

## 六、指定应用限制

这是本项目最核心的能力之一。

管理员可以配置具体应用，例如：

```text
应用名称：
野狐围棋

每日额度：
2小时

匹配规则：
foxwq.exe
foxwqclient.exe
```

Windows Service 持续监控进程。

运行逻辑：

```text
检测进程启动
      ↓
匹配限制规则
      ↓
应用开始运行
      ↓
开始累计使用时间
      ↓
达到每日额度
      ↓
提示用户
      ↓
关闭目标应用
      ↓
当天禁止再次启动
```

---

## 七、多进程共享额度

不能把规则简单设计成：

```text
一个 exe
=
一个独立额度
```

很多 Windows 游戏本身存在多个进程，例如：

```text
Launcher.exe
GameClient.exe
Game.exe
Updater.exe
```

因此需要引入：

**应用组 / App Group**

概念。

例如：

```text
应用组：

野狐围棋

进程：
foxwq.exe
foxwqclient.exe
qqgame.exe

每日额度：
120分钟
```

这些进程共享同一份时间额度。

只要其中任何目标游戏进程正在运行，都认为该应用正在使用。

---

## 八、应用识别方式

不能完全依赖进程名称。

因为软件升级后可能改变 exe 文件名，同时不同目录也可能存在同名程序。

因此建议支持多种识别方式：

### 第一阶段

支持：

- EXE 文件名
- EXE 完整路径

例如：

```text
ProcessName:
foxwq.exe
```

或者：

```text
C:\Program Files\FoxWQ\foxwq.exe
```

### 后续增强

增加：

- 文件 ProductName
- Publisher
- CompanyName
- 文件 Hash
- 父进程
- 数字签名

最终可以建立较稳定的应用识别规则。

---

## 九、应用选择功能

为了避免管理员手动查找 exe 名称，管理端提供：

**选择正在运行的程序**

功能。

页面展示：

```text
进程名称
应用名称
PID
文件路径
图标
```

管理员直接点击：

```text
添加为限制应用
```

即可自动产生应用规则。

同时支持：

**选择 EXE 文件**

从磁盘选择目标应用。

---

## 十、每日额度设计

时间额度统一采用内部秒数保存。

例如：

```text
60分钟
90分钟
120分钟
180分钟
```

数据库实际保存：

```text
7200 秒
```

支持：

```text
每天固定额度

或者

周一 120分钟
周二 120分钟
周三 120分钟
周四 120分钟
周五 120分钟
周六 240分钟
周日 240分钟
```

---

## 十一、额度重置机制

每天自然日重置。

例如：

```text
2026-08-15

野狐围棋

额度：
120分钟

已使用：
95分钟

剩余：
25分钟
```

到了：

```text
2026-08-16 00:00
```

恢复：

```text
已使用：
0分钟

剩余：
120分钟
```

需要注意电脑可能在午夜处于：

- 关机
- 睡眠
- 服务停止

状态。

因此不能单纯依赖定时任务执行重置。

正确方式应该是：

```text
每次读取额度
↓
检查 usageDate
↓
如果 usageDate != 今天
↓
自动创建当天记录
```

这样即使电脑几天没有开机也不会出现额度错误。

---

## 十二、时间统计机制

应用使用时间不能只保存：

```text
程序启动时间
-
程序关闭时间
```

否则遇到：

- 服务重启
- Windows 重启
- 电脑睡眠
- 软件崩溃

容易造成数据错误。

建议 Windows Service 使用周期性统计。

例如：

```text
每 5 秒检查一次目标进程
```

如果进程存在：

```text
usedSeconds += 5
```

并周期性写入数据库。

例如：

```text
每30秒落盘一次
```

同时在：

- 应用退出
- Windows Service 停止
- 系统关机

等事件发生时立即保存。

---

## 十三、提醒机制

为了避免用户在时间耗尽时突然被强制关闭游戏，需要提供提前提醒。

默认提醒：

```text
剩余 30 分钟

剩余 15 分钟

剩余 5 分钟

剩余 1 分钟
```

使用 Windows Notification / Toast 进行通知。

例如：

```text
野狐围棋今日剩余 15 分钟。
```

时间耗尽时：

```text
野狐围棋今日使用时间已达到限制。
```

然后执行终止操作。

---

## 十四、时间耗尽后的应用控制

当指定应用达到每日额度后：

Windows Service 应立即终止目标程序。

并继续监控进程。

如果用户再次运行：

```text
检测到 foxwq.exe
↓
查询今日额度
↓
剩余时间 = 0
↓
立即终止
```

这样就形成：

**当天禁止再次启动**

的效果。

第一阶段可以采用：

```text
Process.Kill()
```

或者 Windows API：

```text
TerminateProcess
```

进行处理。

后期可以进一步研究 Windows：

**Job Object**

机制，提高对子进程和进程树的控制能力。

---

## 十五、管理员保护

否则用户可以直接修改规则或者增加额度。

因此管理端需要：

### 管理员 PIN

打开：

```text
规则设置
额度修改
应用管理
临时增加时间
退出后台服务
```

等敏感功能时，需要输入 PIN。

例如：

```text
管理员 PIN：
******
```

普通用户只能查看：

```text
今日剩余时间
```

---

## 十六、临时时间奖励

管理员可以临时增加当天额度。

例如：

```text
野狐围棋

剩余：
0分钟

[ +15分钟 ]
[ +30分钟 ]
[ +60分钟 ]
```

增加后的时间仅影响当天。

例如：

```text
基础额度：
120分钟

奖励：
30分钟

今日总额度：
150分钟
```

第二天仍然恢复正常：

```text
120分钟
```

---

## 十七、托盘程序

软件默认不需要始终显示窗口。

启动后进入 Windows 系统托盘。

托盘菜单：

```text
防沉迷管理

今日状态

打开管理界面

锁定电脑

退出
```

其中：

**退出**

需要管理员 PIN。

即使退出 UI，也不能停止 Windows Service。

---

# 十八、参考开源项目

本项目不建议完全 Fork 某一个项目，而是分别借鉴不同项目的设计思想。

## 1. LiFE Parental Control

GitHub：

`valueerrorx/LiFE-Parental-Control`

主要借鉴：

- 防沉迷业务模型
- 每日额度
- App Quota
- App Blocking
- 使用统计
- 临时增加时间
- 管理员密码
- GUI 与后台服务分离

这个项目最大的参考价值是：

**应用级每日额度设计。**

其系统不是 Windows，因此不能直接作为项目基础，但业务设计值得重点参考。

---

## 2. Kid PC Monitor

GitHub：

`rookie7799/kid-pc-monitor`

这是 Windows 平台项目。

主要借鉴：

- Windows 后台 Agent
- 每日使用时长限制
- Windows 用户限制
- 重启后继续保存额度
- 使用时间提醒
- 到期锁定电脑

它当前主要偏：

**整机时间控制**

因此非常适合参考本项目的：

**整机限制模块。**

---

## 3. Screen Time Manager for Windows

GitHub：

`spamsch/screen-time-manager-for-windows`

主要借鉴：

- Windows 防沉迷交互设计
- 每周时间配置
- 托盘程序
- 剩余时间显示
- 提前提醒
- 管理员 PIN
- 临时增加时间
- 每日统计
- 时间耗尽锁定

主要参考：

**Windows 客户端体验。**

---

## 4. Process Governor

GitHub：

`lowleveldesign/process-governor`

这是一个 Windows 进程资源与生命周期控制工具。

主要参考：

- Windows Service
- 进程监控
- 进程启动检测
- Job Object
- 子进程管理
- 超时终止进程
- Windows 底层进程控制

本项目后续如果需要提高防绕过能力，可以深入参考它的实现。

---

# 十九、建议技术架构

## 管理客户端

建议：

```text
Tauri 2
+
Vue 3
+
TypeScript
```

UI 可以使用：

```text
Arco Design Vue

或者

Naive UI
```

主要负责：

- 规则配置
- 应用选择
- 时间设置
- 使用记录
- PIN 验证
- 托盘
- 状态展示

---

## Windows Service

推荐：

```text
C#
.NET 10
Worker Service
```

负责：

```text
进程监控
整机使用统计
应用使用统计
规则判断
额度计算
通知
进程终止
Windows 锁定
数据持久化
```

Windows 相关能力 C# / .NET 实现成本相对较低，并且比较容易调用 Win32 API。

---

## 数据存储

采用：

```text
SQLite
```

即可。

软件完全可以：

**本地离线运行。**

第一阶段没有必要：

- 搭服务器
- 做云端账号
- 做远程 API
- 做复杂数据库

---

# 二十、数据模型建议

## LimitRule

```text
id

name

type
COMPUTER / APPLICATION

enabled

mondayLimit

tuesdayLimit

wednesdayLimit

thursdayLimit

fridayLimit

saturdayLimit

sundayLimit
```

---

## ApplicationRule

```text
id

ruleId

applicationName

processName

exePath

productName

publisher
```

一条规则可以绑定多个 ApplicationRule。

---

## DailyUsage

```text
id

ruleId

date

usedSeconds

bonusSeconds
```

例如：

```text
2026-08-15

野狐围棋

usedSeconds:
5400

bonusSeconds:
1800
```

---

# 二十一、第一阶段 MVP

第一阶段不要做得太复杂。

目标是验证：

**限制机制本身是否稳定可靠。**

实现：

- Windows Service
- SQLite
- 应用进程检测
- 指定 EXE 限制
- 每日额度
- 使用时间累计
- 剩余时间计算
- 5 / 1 分钟提醒
- 时间耗尽关闭应用
- 耗尽后禁止再次启动
- 每日自动恢复额度
- 简单管理界面
- 管理员 PIN
- 开机启动

这个版本即可真正投入使用。

---

# 二十二、第二阶段

增加整机控制：

- 每日整机额度
- 用户活动检测
- 锁屏时间不计入
- 系统睡眠时间不计入
- 达到额度锁定 Windows
- 按星期设置不同额度
- 临时增加时间

---

# 二十三、第三阶段

增强使用体验：

- 托盘程序
- Windows Toast 提醒
- 实时剩余时间
- 使用时长图表
- 最近 7 天统计
- 最近 30 天统计
- 应用图标自动读取
- 自动读取 ProductName
- 正在运行程序选择器

---

# 二十四、第四阶段

增强防绕过能力。

考虑以下问题：

```text
用户修改 exe 文件名

用户复制 exe 到其他目录

游戏拉起新的子进程

用户结束管理程序

用户结束后台服务

用户修改系统时间

用户修改 SQLite 数据
```

逐步增加：

- Windows Service 权限保护
- 文件数字签名识别
- Publisher 识别
- ProductName 识别
- Job Object
- 进程树跟踪
- 配置文件完整性校验
- 数据库防篡改
- 系统时间异常检测

这一阶段不建议一开始就做，否则项目复杂度会明显上升。

---

# 二十五、项目实施顺序

建议严格按照以下顺序开发：

```text
第一步
建立 Windows Service

↓

第二步
实现进程扫描与进程事件监听

↓

第三步
识别指定 EXE

↓

第四步
实现使用时间累计

↓

第五步
实现 SQLite 持久化

↓

第六步
实现每日额度

↓

第七步
实现额度耗尽后结束进程

↓

第八步
实现耗尽后再次启动立即阻止

↓

第九步
增加 Windows 通知

↓

第十步
开发管理客户端

↓

第十一步
实现 IPC

↓

第十二步
实现管理员 PIN

↓

第十三步
实现整机时间限制

↓

第十四步
完善安装程序与开机启动
```

其中最核心的验证节点是：

```text
游戏启动
↓
能够识别
↓
准确计时
↓
关闭游戏
↓
游戏重新启动
↓
立即再次关闭
```

只要这一条链路稳定，整个项目的核心已经成立。

---

# 二十六、第一版界面规划

第一版控制在 4 个页面即可。

## 首页

展示：

```text
今日使用情况

电脑使用
1小时35分 / 3小时

野狐围棋
58分钟 / 2小时

QQ游戏大厅
32分钟 / 1小时
```

---

## 限制规则

展示：

```text
电脑使用限制

游戏限制

野狐围棋

QQ游戏大厅
```

支持：

```text
启用 / 禁用

修改额度

编辑应用

删除规则
```

---

## 添加应用

支持：

```text
选择正在运行的程序

选择 EXE 文件
```

自动获取：

```text
应用图标
应用名称
进程名称
安装路径
```

---

## 设置

包括：

```text
管理员 PIN

开机启动

通知设置

数据存储位置

关于软件
```

---

# 二十七、项目范围控制

第一版明确不考虑：

- macOS
- Linux
- 手机端
- Web 管理后台
- 云端同步
- 多设备同步
- 家长账号体系
- 复杂权限系统
- AI 功能
- 网络内容过滤
- 网站过滤
- DNS 过滤

整个项目聚焦于：

> **Windows 本地防沉迷 + 整机时间限制 + 指定应用每日额度限制。**

---

# 二十八、最终目标

最终软件应达到这样的使用体验：

管理员安装软件后，只需要：

```text
添加限制规则
↓
选择“野狐围棋”
↓
设置每天 2 小时
↓
保存
```

之后软件自动完成所有工作。

用户每天可以正常使用游戏：

```text
0 → 2小时
```

期间软件自动统计：

```text
剩余30分钟
剩余15分钟
剩余5分钟
剩余1分钟
```

达到：

```text
2小时
```

以后自动关闭游戏。

当天再次启动：

```text
今日时间额度已使用完毕
```

并自动退出游戏。

第二天：

```text
额度自动恢复为2小时
```

管理员无需每天进行任何操作。

整个软件最终定位可以概括为：

> **一个轻量、本地运行、面向 Windows 的整机与应用级时间配额管理工具。**

第一阶段重点不是实现复杂的“家长控制平台”，而是先把 **Windows 进程识别、准确计时、每日额度和可靠限制** 这四个核心能力做好。